dist: trusty
language: java
jdk:
  - oraclejdk8
cache:
  directories:
  - $HOME/.m2

#install: ./mvnw -B clean validate

script:
  - git clone https://github.com/joinfaces/joinfaces.git
  - ./joinfaces/mvnw install
  - cd ../joinfaces-boot-jetty-cdi/
  - ./mvnw -B -DskipTests=true install
  - ./mvnw -B verify
